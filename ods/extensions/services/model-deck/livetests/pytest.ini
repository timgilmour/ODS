[pytest]
addopts = -ra --strict-markers -p no:cacheprovider
markers =
    safe: reversible drills + read-only checks; runs anytime
    disruptive: parks hipfire / flips routes; only in a declared window
